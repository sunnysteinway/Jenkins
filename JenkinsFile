pipeline {

   agent any

   stages {
       stage('Initialize') {
           steps {
               echo 'Initializing...'
           }
       }
       stage('Tests') {
           
           echo "test"
           
       }
   }
}
